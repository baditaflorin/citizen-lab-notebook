title = "Citizen Lab Notebook gitleaks config"

[extend]
useDefault = true

[allowlist]
description = "Allow documented placeholder values only"
regexes = [
  '''PLACEHOLDER_[A-Z0-9_]+''',
  '''example_(token|key|secret)'''
]
paths = [
  '''docs/assets/transformers\.web-[A-Za-z0-9_-]+\.js'''
]
